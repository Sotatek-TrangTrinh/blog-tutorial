<h1 align="center">HTTP REQUEST </h1>

### Axios vs. fetch(): Which is best for making HTTP requests?

- HTTP: the same
- Backward compatibility: axios is better, axios wide browser support, even old browser
- Response timeout: axios is better, axios has the simplicity of setting a timeout
- Automatic JSON data transformation: axios is better, fetch() doesn't support, you'd have to do it manually
- HTTP interceptors: axios is better, fetch() doesn’t provide a way to intercept requests

Read more in https://blog.logrocket.com/axios-vs-fetch-best-http-requests/
